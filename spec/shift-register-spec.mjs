import ShiftRegister from '../app/js/shift-register.mjs';

describe("shift-register", function() {
  it("contains spec with an expectation", function() {
    new ShiftRegister();
    expect(true).toBe(true);
  });
});
